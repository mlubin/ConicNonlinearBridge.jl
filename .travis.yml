language: julia
os:
  - linux
  - osx
julia:
  - 0.3
  - 0.4
  - nightly
notifications:
  email: false
sudo: false
addons:
    apt_packages:
        - gfortran
# uncomment the following lines to override the default test script
#script:
#  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#  - julia --check-bounds=yes -e 'Pkg.clone(pwd()); Pkg.build("ConicNonlinearBridge"); Pkg.test("ConicNonlinearBridge"; coverage=true)'
